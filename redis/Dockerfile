FROM redis:7-alpine

# 复制自定义 Redis 配置文件（可选）
# COPY redis.conf /usr/local/etc/redis/redis.conf

# 如果需要自定义配置，可以取消注释下面的行
# CMD ["redis-server", "/usr/local/etc/redis/redis.conf"]

# 默认使用基础镜像的 CMD，即 redis-server
# 如果需要持久化，可以在 docker-compose.yml 中配置

